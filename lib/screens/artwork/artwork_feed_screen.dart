import 'package:flutter/material.dart';

class ArtworkFeedScreen extends StatelessWidget {
  const ArtworkFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Artwork Feed — coming soon'),
    );
  }
}